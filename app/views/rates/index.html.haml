#buy_rates
.average
  = "Average USD: #{Rate.day_average('USD', :buy)}"
  = "Average EUR: #{Rate.day_average('EUR', :buy)}"
#sell_rates
.average
  = "Average USD: #{Rate.day_average('USD', :sell)}"
  = "Average EUR: #{Rate.day_average('EUR', :sell)}"
